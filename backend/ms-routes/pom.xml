<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<!-- =========================================== -->
	<!-- PARENT: Spring Boot Starter Parent          -->
	<!-- =========================================== -->
	<!-- Última versión de la rama 3.2.x estable antes de las 4.x.x -->
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.2.12</version>  
		<relativePath />
	</parent>

	<!-- =========================================== -->
	<!-- PROJECT METADATA                            -->
	<!-- =========================================== -->
	<groupId>com.fleetoptimizer</groupId>
	<artifactId>ms-routes</artifactId>
	<version>0.0.1-SNAPSHOT</version>

	<!-- =========================================== -->
	<!-- PROPERTIES: Java version & library versions -->
	<!-- =========================================== -->
	<properties>
		<java.version>17</java.version>
		<org.mapstruct.version>1.6.3</org.mapstruct.version>
		<lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>
		
		<!-- NUEVO: Version de surefire para tests -->
		<maven-surefire-plugin.version>3.5.0</maven-surefire-plugin.version>
	</properties>

	<!-- =========================================== -->
	<!-- DEPENDENCIES                                -->
	<!-- =========================================== -->
	<dependencies>

		<!-- Spring Boot Web Starter -->
		<!-- Includes: Tomcat (embedded), Spring MVC, REST support -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<!-- Spring Data JPA Starter -->
		<!-- Includes: Hibernate (ORM), JPA, Database connectivity -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>

		<!-- PostgreSQL Driver (Recomendado para producción) -->
		<!-- Si usas PostgreSQL en producción, descomenta esto -->
		<!--
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		-->

		<!-- H2 Database (In-memory database for development/testing) -->
		<!-- scope: runtime → only needed at runtime, not for compilation -->
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Lombok: Reduces boilerplate code -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<!-- Spring Boot Test Starter -->
		<!-- Includes: JUnit 5, Mockito, AssertJ, Testcontainers -->
		<!-- scope: test → only needed for testing -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
			<exclusions>
				<!-- Excluir JUnit 4 (usamos JUnit 5) -->
				<exclusion>
					<groupId>org.junit.vintage</groupId>
					<artifactId>junit-vintage-engine</artifactId>
				</exclusion>
			</exclusions>
		</dependency>

		<!-- Bean Validation (Hibernate Validator) -->
		<!-- Annotations: @Valid, @NotBlank, @NotNull, @Size, @Min, @Max, @Pattern -->
		<!-- Required for validating incoming DTOs in controllers -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<!-- Swagger / OpenAPI for API documentation -->
		<!-- Access UI at: http://localhost:8080/swagger-ui.html -->
		<!-- Auto-generates documentation from controllers and DTOs -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>2.6.0</version>
		</dependency>

		<!-- MapStruct: DTO to Entity mapping (compile-time, no reflection) -->
		<!-- Generates type-safe mappers automatically -->
		<!-- Use with @Mapper(componentModel = "spring") -->
		<dependency>
			<groupId>org.mapstruct</groupId>
			<artifactId>mapstruct</artifactId>
			<version>${org.mapstruct.version}</version>
		</dependency>

		<!-- Lombok + MapStruct binding -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok-mapstruct-binding</artifactId>
			<version>${lombok-mapstruct-binding.version}</version>
		</dependency>

		<!-- Testcontainers para integration tests con base de datos real -->
		<!-- Útil si quieres probar con PostgreSQL real en los tests -->
		<!--
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers</artifactId>
			<version>1.19.8</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>postgresql</artifactId>
			<version>1.19.8</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>1.19.8</version>
			<scope>test</scope>
		</dependency>
		-->

	</dependencies>

	<!-- =========================================== -->
	<!-- BUILD CONFIGURATION                         -->
	<!-- =========================================== -->
	<build>
		<plugins>

			<!-- Spring Boot Maven Plugin -->
			<!-- Packages executable JAR with embedded Tomcat -->
			<!-- Excludes Lombok from the final JAR (not needed at runtime) -->
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>

			<!-- Maven Compiler Plugin with Annotation Processors -->
			<!-- Processes Lombok and MapStruct annotations at compile time -->
			<!-- Order matters: Lombok must run before MapStruct -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<!-- Especificar versión de Java explícitamente -->
					<source>17</source>
					<target>17</target>
					<annotationProcessorPaths>
						<!-- Lombok processor: generates getters, setters, builders, logs -->
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>${lombok.version}</version>
						</path>
						<!-- Bridges Lombok annotations with MapStruct -->
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok-mapstruct-binding</artifactId>
							<version>${lombok-mapstruct-binding.version}</version>
						</path>
						<!-- MapStruct processor: generates mapper implementations -->
						<path>
							<groupId>org.mapstruct</groupId>
							<artifactId>mapstruct-processor</artifactId>
							<version>${org.mapstruct.version}</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>

			<!--  Maven Surefire Plugin para tests -->
			<!-- Controla la ejecución de unit tests -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>${maven-surefire-plugin.version}</version>
				<configuration>
					<!-- Incluir tests que terminan en Test o IT -->
					<includes>
						<include>**/*Test.java</include>
						<include>**/*Tests.java</include>
						
					</includes>
					<!--  <include>**/*IT.java</include>
					
					Excluir integration tests de los unit tests
					<excludes>
						<exclude>**/*IT.java</exclude>
					</excludes>  -->
				</configuration>
			</plugin>

			<!-- NUEVO: Maven Failsafe Plugin para integration tests -->
			<!-- Separa los integration tests de los unit tests -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<version>3.5.0</version>
				<configuration>
					<includes>
						<include>**/*IT.java</include>
						<include>**/*IntegrationTest.java</include>
					</includes>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<!-- NUEVO: JaCoCo para cobertura de código -->
			<!-- Mide qué porcentaje de tu código está cubierto por tests -->
			<!--
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>0.8.11</version>
				<executions>
					<execution>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>verify</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			-->

		</plugins>
	</build>

</project>