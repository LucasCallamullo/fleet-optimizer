<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.12</version>
        <relativePath/>
    </parent>

    <groupId>com.fleetoptimizer</groupId>
    <artifactId>gateway</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>gateway</name>
    <description>API Gateway for Fleet Optimization System</description>

    <properties>
        <java.version>17</java.version>
        <spring-cloud.version>2023.0.1</spring-cloud.version>
    </properties>

    <dependencies>
        <!-- ================================================================
             SPRING CLOUD GATEWAY
             ================================================================
             Reactive API Gateway based on Netty.
             Provides routing, filtering, and cross-cutting concerns.
             
             IMPORTANT: Uses WebFlux (reactive) NOT traditional Servlet stack.
             Does NOT include: JPA, JDBC, or Tomcat (uses Netty instead).
             All requests are handled asynchronously and non-blocking.
        -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-gateway</artifactId>
        </dependency>

        <!-- ================================================================
             CIRCUIT BREAKER
             ================================================================
             Resilience4J implementation for fault tolerance.
             Prevents cascading failures with:
             - Timeouts for slow services
             - Fallbacks when services are unavailable
             - Circuit breakers to stop calling failing services
        -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-circuitbreaker-reactor-resilience4j</artifactId>
        </dependency>

        <!-- ================================================================
             ACTUATOR
             ================================================================
             Monitoring and management endpoints.
             Provides:
             - /actuator/health - Service health status
             - /actuator/info - Service information
             - /actuator/gateway - Gateway routes and filters
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- ================================================================
             LOMBOK
             ================================================================
             Reduces boilerplate code.
             Annotations: @Data, @Slf4j, @RequiredArgsConstructor, @Builder.
             Generates code at compile time.
        -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- ================================================================
             SECURITY & JWT
             ================================================================
             OAuth2 Resource Server: Validates JWT tokens from Keycloak.
             Provides JWT authentication for all incoming requests.
             Required for the gateway to validate tokens before routing.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <!-- Spring Security: Core security framework.
             Provides SecurityContext, authentication, and authorization.
             Required for @PreAuthorize and method-level security.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- ================================================================
             TESTING
             ================================================================
             Spring Boot Test: Comprehensive testing framework.
             Includes: JUnit 5, Mockito, and Spring TestContext.
             Excludes JUnit 4 vintage engine for clean JUnit 5 setup.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.junit.vintage</groupId>
                    <artifactId>junit-vintage-engine</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
    </dependencies>

    <!-- ================================================================
         DEPENDENCY MANAGEMENT
         ================================================================
         Manages Spring Cloud versions for all cloud dependencies.
         This ensures all cloud components use compatible versions.
    -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <!-- Compiler Plugin: Configures annotation processing.
                 Required for Lombok to generate code at compile time.
                 Specifies Lombok version explicitly to avoid build issues.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <!-- Explicit version prevents compile-time issues -->
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <!-- Spring Boot Maven Plugin: Creates executable JAR.
                 Excludes Lombok from the final JAR (not needed at runtime).
            -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>