# ================================================================
# APPLICATION - ms-auth (Test configuration)
# ================================================================
# Used when running tests with the "test" profile.
# ================================================================

logging:
  level:
    com.auth: WARN